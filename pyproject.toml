[project]
name = "lesionnet"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shithil17", email = "shuhadashithil17@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "torch",
    "torchvision",
    "pillow",
    "pandas",
    "numpy",
    "scikit-learn",
    "tqdm",
    "matplotlib",
    "seaborn",
    "timm>=1.0.28",
]

[project.scripts]
lesionnet = "lesionnet:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
